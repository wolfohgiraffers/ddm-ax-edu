# 동대문구시설관리공단 AX 교육 모집페이지 제작·운영 가이드

최종 정리일: 2026-09-01

## 1. 관련 대화 세션

이 페이지와 관련된 핵심 대화는 두 개입니다.

| 구분 | 대화 제목 | ID | 역할 |
|---|---|---|---|
| 실제 제작·수정·배포 | `제작 8H AI 교육 랜딩페이지` | `01a041f8-773b-7213-8b77-cf148720f45f` | 사이트 구현, 반응형 검수, Google Form 연결, GitHub 배포 |
| 초기 기획 | `모집 페이지 기획` | `6a8fdc1f-1b94-83e8-8d31-1e5f1e79f29a` | 페이지 흐름, CTA, A/B반 운영 방식, Google Form·Sheet 구조 기획 |

실제 파일을 다시 수정하거나 배포해야 할 때는 **「제작 8H AI 교육 랜딩페이지」 세션**을 이어서 사용하면 됩니다.

## 2. 현재 운영 주소

- GitHub 저장소: <https://github.com/wolfohgiraffers/ddm-ax-edu>
- 모집페이지: <https://wolfohgiraffers.github.io/ddm-ax-edu/>
- 신청 Google Form: <https://docs.google.com/forms/d/e/1FAIpQLSevweTdISJvqwFwH-O7BPtTcyw8P7jDllSS3wihY-qeQJCODQ/viewform>

Google Form 편집 주소, 응답 Sheet, 운영 Drive 폴더 주소는 개인정보 보호를 위해 공개 저장소에 기록하지 않습니다.

## 3. 페이지의 목적과 핵심 정보

이 페이지는 동대문구시설관리공단 임직원을 대상으로 하는 다음 교육의 모집용 랜딩페이지입니다.

- 과정명: `AX 레시피 - 생성형 AI 활용`
- 일정: 2026년 9월 29일(화)~30일(수)
- A반: 09:00~13:00, 이틀간 총 8시간
- B반: 14:00~18:00, 이틀간 총 8시간
- 대상: 생성형 AI를 처음 사용하는 사람을 포함한 전 직군 실무자
- 신청 방식: 페이지의 신청 버튼 → Google Form → 비공개 Google Sheets 자동 집계

16시간 `업무 프로세스 개선` 과정은 이 페이지에 포함하지 않습니다.

## 4. 페이지 구성 원칙

페이지는 다음 순서로 읽히도록 설계했습니다.

1. 첫 화면에서 날짜·시간·총 8시간·A/B반 선택 가능 여부 확인
2. AI 초급자도 참여할 수 있다는 안내
3. 강사 시연 → 함께 실습 → 내 업무 적용 흐름 소개
4. 8시간 학습 과정과 교시별 상세 커리큘럼
5. ChatGPT·Claude·Gemini 선택 안내
6. A반·B반 일정 비교
7. 교육 후 가져가는 결과물
8. FAQ
9. 최종 신청 CTA

페이지 문구는 과도하게 광고성으로 보이지 않되, `내 업무 한 가지 바꿔보기`, `지금 교육 신청하기`처럼 다음 행동이 분명하게 보이도록 작성했습니다.

## 5. 파일 구성

| 파일·폴더 | 용도 |
|---|---|
| `index.html` | 페이지의 모든 문구, 일정, 커리큘럼, 버튼 링크와 화면 구조 |
| `styles.css` | 색상, 글꼴, 간격, 반응형 화면, 카드와 CTA 디자인 |
| `script.js` | 커리큘럼·FAQ를 하나씩 열고 닫는 동작 |
| `assets/` | 교육 사진, 삽화, 로고, SNS 공유 이미지 |
| `assets/fonts/` | 페이지에 직접 포함한 한글 웹폰트와 라이선스 |
| `create_google_form.gs` | Google Form·응답 Sheet 생성 및 설문 교체용 Apps Script |
| `GOOGLE_FORM_SETUP.md` | 설문 문항과 Sheet 구성 설명 |
| `PROJECT_LINKS.md` | 공개 가능한 운영 링크와 권한 원칙 |
| `GITHUB_PAGES_DEPLOY.md` | GitHub 웹 화면을 이용한 기본 배포 절차 |

별도의 서버나 데이터베이스 없이 GitHub Pages에서 실행되는 정적 사이트입니다.

## 6. 자주 수정하는 항목

### 일정과 반 시간 변경

`index.html`에서 기존 날짜 또는 시간을 검색해 모두 바꿉니다.

- `9.29.(화)–30.(수)`
- `09:00–13:00`
- `14:00–18:00`
- `총 8시간`

첫 화면, 일정 카드, FAQ, 최종 CTA에 같은 정보가 반복되므로 한 곳만 바꾸면 안 됩니다. 수정 후 페이지 전체에서 이전 날짜가 남아 있지 않은지 검색합니다.

### 교육 문구와 커리큘럼 변경

`index.html`에서 해당 섹션의 문구를 수정합니다. 교시별 내용은 `<details>` 안에 있으며, 사용자가 제목을 누르면 상세 내용이 열립니다.

새 문구가 길어졌다면 휴대전화에서 제목이 지나치게 여러 줄로 나뉘지 않는지도 확인합니다.

### Google Form 주소 변경

`index.html`에서 현재 Google Form 주소를 새 주소로 **전체 바꾸기** 합니다. 모든 신청 버튼에는 `js-form-link` 클래스가 들어 있습니다.

변경 후 다음 위치의 버튼을 각각 확인합니다.

- 상단 신청 버튼
- 첫 화면 메인 버튼
- A반 신청 버튼
- B반 신청 버튼
- 최종 신청 버튼

### 사진과 로고 교체

새 파일을 `assets/`에 넣고 `index.html`의 이미지 경로를 바꿉니다.

- 공단 로고: `assets/dfmc-logo.png`
- 헬로월드랩스 로고: `assets/helloworldlabs-logo.png`
- 첫 교육 사진: `assets/classroom-wide.jpg`
- 두 번째 교육 사진: `assets/classroom-close.jpg`
- 초급자 삽화: `assets/beginner-journey.png`
- 학습 흐름 삽화: `assets/learning-path.png`
- SNS 공유 이미지: `assets/og.png`

GitHub Pages는 파일명의 대소문자를 구분하므로 파일명과 HTML 경로를 정확하게 맞춰야 합니다.

### 색상과 글꼴 변경

주요 색상은 `styles.css` 맨 위 `:root` 영역에서 관리합니다.

- `--forest`: 핵심 청록색
- `--coral`: 강조색과 CTA
- `--yellow`: 밑줄과 포스트잇
- `--paper`, `--cream`: 배경색

본문·제목은 자체 포함한 Pretendard Variable, 손글씨 메모는 Nanum Pen Script를 사용합니다. `assets/fonts/` 안의 글꼴 파일을 삭제하면 화면 글꼴이 달라질 수 있습니다.

현재 글꼴은 페이지에 사용된 글자 중심으로 경량화되어 있습니다. 새로운 한글 문구를 대량으로 추가했는데 일부 글자만 다른 글꼴로 보이면 웹폰트 부분집합을 다시 만들거나 전체 글꼴 파일로 교체해야 합니다.

### CSS 캐시 갱신

디자인을 수정했는데 배포 후 이전 디자인이 계속 보이면 `index.html`의 다음 숫자를 새 값으로 바꿉니다.

```html
<link rel="stylesheet" href="styles.css?v=새버전">
```

예: `styles.css?v=20260901-1`

이 값은 브라우저가 새 CSS를 다시 내려받도록 하는 용도입니다.

## 7. Google Form과 응답 Sheet 운영

### 최초 생성

새로운 Form·Sheet·Drive 폴더를 처음 만들 때만 Apps Script에서 `createAxRecipeForm` 함수를 실행합니다.

실행하면 다음 항목이 생성됩니다.

1. 교육 운영용 비공개 Drive 폴더
2. 교육 신청 및 사전 수요조사 Google Form
3. 신청자 응답용 Google Spreadsheet
4. Spreadsheet 안의 운영 안내 시트

이미 현재 Form과 Sheet가 존재하므로 같은 교육에서 이 함수를 다시 실행하면 중복 폴더와 중복 Form이 생깁니다.

### 기존 설문 문항 교체

현재 Form의 주소와 Spreadsheet를 유지하면서 문항만 바꿀 때는 `updateAxRecipeFormFromSurvey`를 사용합니다.

이 함수는 응답 Sheet에 실제 응답이 있으면 자동으로 중단됩니다. 신청자가 있는 상태에서 문항과 열을 임의로 바꾸지 말고 먼저 응답을 백업해야 합니다.

### 응답 시트 이름

- 아래쪽 시트 탭 이름: `AX레시피_신청응답_2026`
- 표 위쪽에 보이는 `Form_Responses`: Google Sheets의 표 이름

둘은 서로 다른 이름입니다. Form 연결 후 시트 탭 이름을 바꾸더라도 연결은 일반적으로 유지되며, 새 응답은 연결된 응답 탭에 계속 추가됩니다.

실제 모집 시작 전에는 테스트 응답 한 건을 제출하여 새 행이 생기는지 확인하고, 확인 후 테스트 행만 삭제합니다.

### 반드시 유지할 권한

- Google Form: 링크를 받은 교육 대상자가 응답 가능
- 운영 Drive 폴더: 소유자와 지정 운영자만 접근
- 응답 Google Sheets: 소유자와 지정 운영자만 접근
- 신청자 개인정보가 들어간 폴더·Sheet: `링크가 있는 모든 사용자` 공개 금지

랜딩페이지와 Google Form은 공개될 수 있지만, 응답 Sheet는 공개하면 안 됩니다.

## 8. 로컬에서 확인하는 방법

가장 간단한 방법은 `index.html`을 브라우저로 여는 것입니다.

개발용 웹서버가 필요하면 저장소 폴더에서 다음처럼 실행할 수 있습니다.

```powershell
python -m http.server 4175
```

그다음 브라우저에서 `http://localhost:4175/`에 접속합니다.

## 9. GitHub Pages 배포

현재 저장소는 `main` 브랜치의 루트 폴더를 GitHub Pages로 배포합니다. 파일을 커밋하고 `main`에 올리면 자동으로 재배포됩니다.

### Git 사용 시

```powershell
git status
git add index.html styles.css script.js assets
git commit -m "Update AX education landing page"
git push origin main
```

문서만 수정했다면 해당 문서만 추가해도 됩니다.

### GitHub 웹 화면 사용 시

1. 저장소에서 `Add file` → `Upload files` 선택
2. 수정 파일을 기존 위치에 업로드
3. `Commit changes` 선택
4. 1~3분 후 모집페이지에서 새로고침

배포 설정은 저장소의 `Settings → Pages → Deploy from a branch → main / (root)`입니다.

## 10. 배포 전 검수표

### 내용

- [ ] 과정명과 일정이 정확하다.
- [ ] A반·B반 시간이 첫 화면과 일정 카드에서 동일하다.
- [ ] 총 교육시간이 8시간으로 표시된다.
- [ ] 16시간 업무 프로세스 개선 과정 내용이 섞이지 않았다.
- [ ] 초급자도 참여할 수 있다는 안내가 보인다.
- [ ] 공단명과 교육 운영기관명이 정확하다.

### 기능

- [ ] 모든 신청 버튼이 같은 Google Form으로 이동한다.
- [ ] 교시를 누르면 상세 커리큘럼이 열리고 다른 교시는 닫힌다.
- [ ] FAQ가 정상적으로 열린다.
- [ ] 테스트 Form 응답이 Sheet에 새 행으로 들어간다.

### 화면

- [ ] 320~360px 휴대전화에서 가로 스크롤이 생기지 않는다.
- [ ] 상단에서 공단 로고와 헬로월드랩스 로고가 모두 보인다.
- [ ] 제목·사진·캡션이 서로 겹치지 않는다.
- [ ] 일정 카드의 시간과 신청 버튼이 잘리지 않는다.
- [ ] 커리큘럼을 펼쳤을 때 글자가 기울거나 겹치지 않는다.
- [ ] 태블릿 세로 화면에서 과도한 빈 공간이 생기지 않는다.
- [ ] 포스트잇·밑줄 장식이 글자를 가리지 않는다.

### 접근성과 안전

- [ ] 이미지에 적절한 대체 텍스트가 있다.
- [ ] 키보드만으로 버튼과 커리큘럼을 사용할 수 있다.
- [ ] 개인정보 Sheet와 Drive 폴더가 공개되어 있지 않다.
- [ ] 공개 저장소에 Form 편집 주소나 Sheet 편집 주소를 기록하지 않았다.

## 11. 모집 시작·종료 운영

### 모집 시작 전

1. 일정·분반·문구 최종 확인
2. 모든 CTA의 Form 주소 확인
3. Form 테스트 응답 제출
4. 응답 Sheet 열과 권한 확인
5. 테스트 행 삭제
6. 모바일과 PC에서 최종 확인
7. 모집 URL 배포

### 모집 중

- Form 문항과 Sheet 열 순서를 임의로 변경하지 않습니다.
- A/B반 인원과 연락처는 공개 페이지가 아니라 비공개 Sheet에서 관리합니다.
- 교육 도구 선호도와 사전설문은 강사가 수업 예시와 속도를 조정하는 용도로만 사용합니다.

### 모집 종료 후

1. Google Form에서 응답 받기를 중지합니다.
2. 최종 명단을 권한이 제한된 위치에 보관합니다.
3. 보유 기간이 지난 개인정보는 기관 기준에 따라 파기합니다.
4. 다음 교육에 재사용할 때는 기존 신청자 데이터를 그대로 복제하지 않습니다.

## 12. 문제 발생 시 빠른 확인

| 증상 | 확인할 내용 |
|---|---|
| 수정했는데 화면이 그대로임 | GitHub Pages 배포 완료 여부, 강력 새로고침, CSS 버전 값 |
| 신청 버튼이 안 열림 | `index.html`의 Form URL과 `href` 값 |
| 이미지가 안 보임 | `assets/` 경로, 파일명과 대소문자 |
| 모바일에서 겹침 | `styles.css`의 `@media(max-width:800px)`와 `@media(max-width:560px)` |
| Form 응답이 안 들어옴 | Form의 응답 대상 Spreadsheet 연결, 응답 받기 활성화 여부 |
| 새 응답 탭이 생김 | Form을 다시 연결했는지 확인하고 실제 연결 탭을 테스트 응답으로 확인 |
| 일부 새 글자만 다른 글꼴임 | 경량화 웹폰트에 새 글자가 포함되어 있는지 확인 |

## 13. 변경 이력과 되돌리기

GitHub의 각 커밋은 이전 상태를 보관합니다. 문제가 발생하면 변경 파일과 직전 정상 커밋을 비교한 뒤 필요한 부분만 되돌립니다.

작업 전에는 항상 `git status`로 다른 사람의 미완료 변경이 있는지 먼저 확인하고, 관련 없는 파일은 덮어쓰지 않습니다.

